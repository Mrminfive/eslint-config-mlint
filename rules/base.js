module.exports = {
    rules: {
        // 是否启用句尾分号，默认关闭
        semi: [2, 'never'],

        // 使用 4 空格缩紧分隔
        indent: [2, 4],

        // 取消强制用逗号分隔的多行结构，始终加上最后一个逗号
        'comma-dangle': 'off'
    }
}
